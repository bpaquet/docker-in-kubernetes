# Project Rules

- Always ask questions before implementing.
- Keep a `Design.md` file up to date.
- Do not write long comments in the code. Default to no comment. Add one only when the *why* is non-obvious (hidden constraint, subtle invariant, workaround). Never explain *what* the code does — names should do that. Doc comments on exported identifiers stay short: one line where possible.
